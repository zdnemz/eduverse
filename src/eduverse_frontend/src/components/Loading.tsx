export default function Loading() {
  return (
    <main className="flex h-screen w-full items-center justify-center bg-black/50">
      <h2 className="font-semibold">Loading...</h2>
    </main>
  );
}
